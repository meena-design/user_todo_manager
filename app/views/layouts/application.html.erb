<!DOCTYPE html>
<html>
  <head>
    <title>TodoManager</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", media: "all", 'data-turbolinks-track': "reload" %>
    <%= javascript_pack_tag "application", 'data-turbolinks-track': "reload" %>
  </head>

  <body>
  <!-- the below line is a header for every page
  <h1 style="background-color: darkblue; color: white;">The Todo Application</h1> -->
  <% if @current_user %>
  <p class="UserHeader">
    Welcome <%= @current_user.first_name %>
    <%= link_to "Sign-out", destroy_session_path, method: "delete" %>
  </p>
  <% end %>
  <% if flash[:error] %>
<p class="Flash-error">
  <%= flash[:error] %>
  </p>
  <% end %>
    <%= yield %>
  <!-- the below line is a footer
  <hr>
  <p style="font-size: 32px;"> TodoManager App (C) The Todo Company </p> -->
  </body>
</html>
